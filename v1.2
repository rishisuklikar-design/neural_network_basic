import math

x = float(input("x: "))
y = float(input("y: "))

def relu(z):
    return max(0, z)

# hidden layer
h1 = relu(1.2*x + 0.8*y - 6)
h2 = relu(-1.0*x + 1.1*y - 3)
h3 = relu(0.7*x - 1.3*y - 2)

z = 1.5*h1 - 1.2*h2 + 0.8*h3 - 0.5

if z > 0:
    print("class 1")
else:
    print("class 2")
