# neural_network_basic
A basic neural network, more complex version will be added slowly

Version 1:
a classification model, based on a scatter graph with 12 points, split in 2 using the line of best fit(linear) but in a negative slope.
